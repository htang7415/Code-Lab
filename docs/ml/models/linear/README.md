# Linear Models

Linear models are the baseline family for prediction, calibration, and interpretation.

## Current Anchors

- Linear regression (`modules/ml/models/linear-regression`)
- Logistic regression (`modules/ml/models/logistic-regression`)
- Softmax regression (`modules/ml/models/softmax-regression`)
- Elastic Net (`modules/ml/models/elastic-net`)
- SVM with linear-style margin intuition (`modules/ml/models/svm-pegasos`)

## Concepts to Cover Well

- Regression vs classification objectives
- Regularization with L1, L2, and Elastic Net
- Decision boundaries and margin intuition
- Feature engineering and polynomial expansion
- When linear baselines are strong enough to beat more complex models
