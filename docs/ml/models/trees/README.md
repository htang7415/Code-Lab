# Trees and Ensembles

Tree-based models dominate many tabular problems because they capture nonlinear structure with limited preprocessing.

## Current Anchors

- Tree and ensemble methods (`modules/ml/models/tree-ensemble-methods`)
- Isolation Forest (`modules/ml/models/isolation-forest`)

## Concepts to Cover Well

- Split quality via impurity or loss reduction
- Variance reduction from bagging
- Bias reduction from boosting
- Feature importance and pruning
- When trees beat linear models on tabular data
